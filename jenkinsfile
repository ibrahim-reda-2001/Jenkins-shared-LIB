@Library('SharedLibraryNTI@master') _
pipeline {
    
agent any
    stages {
        stage('Test') {
            steps {
                script {
                    buildandpushDocker(
                        dockerCredentialsID: 'docker-hub-cred',
                        repoName: 'docker.io/test',
                        imageName: 'test-image',
                        dockerfile: 'Dockerfile'
                    )
                }
            }
        }
    }
}